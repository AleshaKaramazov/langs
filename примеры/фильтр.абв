

Алгоритм Главная() Начало
    пусть числа: Массив<Нат> = Массив![1,2,3,4,5,6,7,8];
    пусть четные = Фильтр(числа, |ф: Нат| ф % 2 == 0);
    Написать!(четные);
Конец

Алгоритм Фильтр(
    масс: Массив<Нат>,
    услов: Лямбда<Нат> -> Логич
) -> Массив<Нат> 
Начало
    пусть ответ: Массив<Нат> = Массив![];
    для всех(т) в массиве(масс) выполнить
        если услов(т) то 
            ответ.Добавить(т);

    вернуть ответ; 
Конец
